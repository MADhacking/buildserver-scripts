Modify everything to use q where available

Check for version change of key packages before performing related update checks
	qlist -I -S dev-lang/perl -v
	portageq best_visible /mnt/portage/portage sys-devel/gcc
	portageq pkgdir
	portageq distdir
	portageq portdir

Kernel			preserve existing versions									DONE
Linux headers	glibc rebuild, perl headers rebuild (done)					DONE
Perl			perl-cleaner												DONE
Python			python-updater												DONE
GCC				preserve existing versions									DONE

delete-redundant-libs
	should rebuild packages _containing_ preserved libraries				DONE
	
